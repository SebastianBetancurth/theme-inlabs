{% comment %}
  ============================================================
  SECCIÓN: Testimonios con transición vertical
  Theme: Dawn (compatible con cualquier theme OS 2.0)
  Archivo: sections/testimonials-slide.liquid
  ------------------------------------------------------------
  - Slider de reseñas: la reseña actual se desliza hacia ARRIBA
    desapareciendo con fade, y la nueva entra desde ABAJO
    (al ir hacia atrás, la dirección se invierte).
  - Estrellas configurables por reseña (1 a 5).
  - Cita en richtext inline (permite negritas) + autor.
  - Flechas circulares + puntos de navegación.
  - Autoplay opcional con pausa al pasar el mouse.
  - TODO editable desde el customizer: colores, textos,
    tamaños, paddings, márgenes y espacios internos,
    con controles separados para escritorio y móvil.
  ============================================================
{% endcomment %}

{%- liquid
  assign sid = section.id
-%}

{%- style -%}
  #shopify-section-{{ sid }} .zt-section {
    /* display:block es OBLIGATORIO: los custom elements son
       inline por defecto y el fondo/padding no se aplican */
    display: block;
    --zt-bg: {{ section.settings.color_bg }};
    --zt-text: {{ section.settings.color_text }};
    --zt-author: {{ section.settings.color_author }};
    --zt-star: {{ section.settings.color_star }};
    --zt-nav: {{ section.settings.color_nav }};
    --zt-nav-border: {{ section.settings.color_nav_border }};
    --zt-dot: {{ section.settings.color_dot }};
    --zt-dot-active: {{ section.settings.color_dot_active }};
    --zt-quote-size: {{ section.settings.quote_size }}px;
    --zt-quote-size-mobile: {{ section.settings.quote_size_mobile }}px;
    --zt-author-size: {{ section.settings.author_size }}px;
    --zt-star-size: {{ section.settings.star_size }}px;
    --zt-star-size-mobile: {{ section.settings.star_size_mobile }}px;
    --zt-max-width: {{ section.settings.max_width }}px;
    padding-top: {{ section.settings.padding_top }}px;
    padding-bottom: {{ section.settings.padding_bottom }}px;
    margin-top: {{ section.settings.margin_top }}px;
    margin-bottom: {{ section.settings.margin_bottom }}px;
    background: var(--zt-bg);
    overflow: hidden;
  }

  #shopify-section-{{ sid }} .zt-wrapper {
    max-width: var(--zt-max-width);
    margin: 0 auto;
    padding: 0 {{ section.settings.padding_x }}px;
    text-align: center;
    font-family: var(--font-body-family);
  }

  /* ============ ESCENARIO (las slides van apiladas) ============ */
  #shopify-section-{{ sid }} .zt-stage {
    position: relative;
    transition: height .35s ease;
  }
  #shopify-section-{{ sid }} .zt-slide {
    position: absolute;
    inset: 0 0 auto 0;
    opacity: 0;
    visibility: hidden;
    pointer-events: none;
  }
  #shopify-section-{{ sid }} .zt-slide.is-active {
    opacity: 1;
    visibility: visible;
    pointer-events: auto;
  }

  /* ============ TRANSICIÓN SUAVE (crossfade + blur) ============ */
  /* La reseña actual se desvanece difuminándose, y la nueva
     aparece enfocándose desde el desenfoque con un zoom sutil.
     Sin movimientos bruscos: elegante y cinemático. */
  #shopify-section-{{ sid }} .zt-slide.zt-out {
    animation: zt-fade-out .5s ease both;
  }
  #shopify-section-{{ sid }} .zt-slide.zt-in {
    animation: zt-fade-in .75s cubic-bezier(.22, .9, .32, 1) both;
    animation-delay: .18s;
  }
  @keyframes zt-fade-out {
    from {
      opacity: 1;
      filter: blur(0);
      transform: scale(1);
    }
    to {
      opacity: 0;
      filter: blur(10px);
      transform: scale(.985);
    }
  }
  @keyframes zt-fade-in {
    from {
      opacity: 0;
      filter: blur(8px);
      transform: scale(1.015);
    }
    to {
      opacity: 1;
      filter: blur(0);
      transform: scale(1);
    }
  }

  /* ============ CONTENIDO DE LA SLIDE ============ */
  #shopify-section-{{ sid }} .zt-stars {
    display: flex;
    justify-content: center;
    gap: .8rem;
    margin-bottom: {{ section.settings.gap_stars }}px;
    color: var(--zt-star);
  }
  #shopify-section-{{ sid }} .zt-stars svg {
    width: var(--zt-star-size);
    height: var(--zt-star-size);
    fill: currentColor;
  }
  #shopify-section-{{ sid }} .zt-quote {
    font-size: var(--zt-quote-size);
    line-height: 1.25;
    font-weight: 300;
    color: var(--zt-text);
    margin: 0 auto {{ section.settings.gap_quote }}px;
    max-width: 90rem;
    {% if section.settings.uppercase %}text-transform: uppercase;{% endif %}
  }
  #shopify-section-{{ sid }} .zt-quote strong,
  #shopify-section-{{ sid }} .zt-quote b {
    font-weight: 700;
  }
  #shopify-section-{{ sid }} .zt-author {
    font-size: var(--zt-author-size);
    color: var(--zt-author);
    margin: 0;
  }

  /* ============ NAVEGACIÓN ============ */
  #shopify-section-{{ sid }} .zt-nav {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 2rem;
    margin-top: {{ section.settings.gap_nav }}px;
  }
  #shopify-section-{{ sid }} .zt-nav-btn {
    width: 4.4rem;
    height: 4.4rem;
    border-radius: 50%;
    border: 1px solid var(--zt-nav-border);
    background: transparent;
    color: var(--zt-nav);
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    flex: 0 0 auto;
    transition: background .2s ease, color .2s ease;
  }
  #shopify-section-{{ sid }} .zt-nav-btn svg {
    width: 1rem;
    height: 1.6rem;
  }
  #shopify-section-{{ sid }} .zt-nav-btn:hover {
    background: var(--zt-nav);
    color: var(--zt-bg);
  }
  #shopify-section-{{ sid }} .zt-dots {
    display: flex;
    gap: 1.1rem;
  }
  #shopify-section-{{ sid }} .zt-dot {
    width: .7rem;
    height: .7rem;
    border-radius: 50%;
    border: 0;
    padding: 0;
    background: var(--zt-dot);
    cursor: pointer;
    transition: background .25s ease, transform .25s ease;
  }
  #shopify-section-{{ sid }} .zt-dot[aria-current='true'] {
    background: var(--zt-dot-active);
    transform: scale(1.35);
  }

  /* ============ MOBILE ============ */
  @media screen and (max-width: 749px) {
    #shopify-section-{{ sid }} .zt-section {
      padding-top: {{ section.settings.padding_top_mobile }}px;
      padding-bottom: {{ section.settings.padding_bottom_mobile }}px;
      margin-top: {{ section.settings.margin_top_mobile }}px;
      margin-bottom: {{ section.settings.margin_bottom_mobile }}px;
    }
    #shopify-section-{{ sid }} .zt-wrapper {
      padding: 0 {{ section.settings.padding_x_mobile }}px;
      {% if section.settings.align_left_mobile %}text-align: left;{% endif %}
    }
    {% if section.settings.align_left_mobile %}
      #shopify-section-{{ sid }} .zt-stars { justify-content: flex-start; }
      #shopify-section-{{ sid }} .zt-quote { margin-left: 0; margin-right: 0; }
    {% endif %}
    #shopify-section-{{ sid }} .zt-stars {
      margin-bottom: {{ section.settings.gap_stars_mobile }}px;
    }
    #shopify-section-{{ sid }} .zt-stars svg {
      width: var(--zt-star-size-mobile);
      height: var(--zt-star-size-mobile);
    }
    #shopify-section-{{ sid }} .zt-quote {
      font-size: var(--zt-quote-size-mobile);
      margin-bottom: {{ section.settings.gap_quote_mobile }}px;
      max-width: 100%;
    }
    #shopify-section-{{ sid }} .zt-nav {
      margin-top: {{ section.settings.gap_nav_mobile }}px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    #shopify-section-{{ sid }} .zt-slide.zt-out,
    #shopify-section-{{ sid }} .zt-slide.zt-in {
      animation: none;
    }
    #shopify-section-{{ sid }} .zt-stage { transition: none; }
  }
{%- endstyle -%}

<zi-testimonials
  class="zt-section"
  data-section-id="{{ sid }}"
  {% if section.settings.autoplay %}data-autoplay="{{ section.settings.autoplay_speed | times: 1000 }}"{% endif %}
>
  <div class="zt-wrapper">
    <div class="zt-stage" role="region" aria-roledescription="carrusel" aria-label="Reseñas de clientes">
      {%- for block in section.blocks -%}
        <div
          class="zt-slide{% if forloop.first %} is-active{% endif %}"
          data-slide
          role="group"
          aria-roledescription="reseña"
          aria-label="Reseña {{ forloop.index }} de {{ forloop.length }}"
          {{ block.shopify_attributes }}
        >
          {%- if block.settings.rating > 0 -%}
            <div class="zt-stars" aria-label="{{ block.settings.rating }} de 5 estrellas">
              {%- for i in (1..block.settings.rating) -%}
                <svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
                  <path d="M12 1.6l3.1 6.9 7.5.8-5.6 5.1 1.5 7.4L12 18l-6.5 3.8 1.5-7.4L1.4 9.3l7.5-.8L12 1.6z"/>
                </svg>
              {%- endfor -%}
            </div>
          {%- endif -%}
          {%- if block.settings.quote != blank -%}
            <p class="zt-quote">{{ block.settings.quote }}</p>
          {%- endif -%}
          {%- if block.settings.author != blank -%}
            <p class="zt-author">{{ block.settings.author | escape }}</p>
          {%- endif -%}
        </div>
      {%- endfor -%}
    </div>

    {%- if section.blocks.size > 1 -%}
      <div class="zt-nav">
        <button type="button" class="zt-nav-btn" data-dir="-1" aria-label="Reseña anterior">
          <svg viewBox="0 0 10 16" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
            <path d="M8.5 1L1.5 8L8.5 15" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </button>
        <div class="zt-dots" role="tablist" aria-label="Elegir reseña">
          {%- for block in section.blocks -%}
            <button
              type="button"
              class="zt-dot"
              data-dot="{{ forloop.index0 }}"
              aria-label="Ir a la reseña {{ forloop.index }}"
              aria-current="{% if forloop.first %}true{% else %}false{% endif %}"
            ></button>
          {%- endfor -%}
        </div>
        <button type="button" class="zt-nav-btn" data-dir="1" aria-label="Reseña siguiente">
          <svg viewBox="0 0 10 16" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
            <path d="M1.5 1L8.5 8L1.5 15" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </button>
      </div>
    {%- endif -%}
  </div>
</zi-testimonials>

<script>
  if (!customElements.get('zi-testimonials')) {
    customElements.define(
      'zi-testimonials',
      class ZiTestimonials extends HTMLElement {
        connectedCallback() {
          this.stage = this.querySelector('.zt-stage');
          this.slides = Array.from(this.querySelectorAll('[data-slide]'));
          this.dots = Array.from(this.querySelectorAll('[data-dot]'));
          this.current = 0;
          this.animating = false;
          this.ANIM_MS = 950; /* .75s de entrada + .18s de delay + margen */

          if (this.slides.length === 0) return;

          /* Altura del escenario = altura de la slide activa
             (las slides están en position:absolute) */
          this.setStageHeight();
          this.onResize = () => this.setStageHeight();
          window.addEventListener('resize', this.onResize);

          /* Flechas */
          this.querySelectorAll('.zt-nav-btn').forEach((btn) => {
            btn.addEventListener('click', () => {
              const dir = parseInt(btn.dataset.dir, 10);
              const next = (this.current + dir + this.slides.length) % this.slides.length;
              this.goTo(next);
              this.restartAutoplay();
            });
          });

          /* Puntos */
          this.dots.forEach((dot) => {
            dot.addEventListener('click', () => {
              const index = parseInt(dot.dataset.dot, 10);
              this.goTo(index);
              this.restartAutoplay();
            });
          });

          /* Autoplay opcional, con pausa al pasar el mouse */
          this.autoplayMs = parseInt(this.dataset.autoplay || '0', 10);
          if (this.autoplayMs > 0 && this.slides.length > 1) {
            this.startAutoplay();
            this.addEventListener('mouseenter', () => this.stopAutoplay());
            this.addEventListener('mouseleave', () => this.startAutoplay());
          }
        }

        setStageHeight() {
          const active = this.slides[this.current];
          if (active) this.stage.style.height = active.offsetHeight + 'px';
        }

        goTo(index) {
          if (this.animating || index === this.current) return;
          this.animating = true;

          const out = this.slides[this.current];
          const inn = this.slides[index];

          /* La entrante se activa y aparece; la saliente se desvanece */
          inn.classList.add('is-active', 'zt-in');
          out.classList.add('zt-out');

          /* Ajustar la altura del escenario a la slide entrante */
          this.stage.style.height = inn.offsetHeight + 'px';

          /* Puntos */
          this.dots.forEach((d, i) => d.setAttribute('aria-current', i === index ? 'true' : 'false'));

          this.current = index;

          setTimeout(() => {
            out.classList.remove('is-active', 'zt-out');
            inn.classList.remove('zt-in');
            this.animating = false;
          }, this.ANIM_MS);
        }

        startAutoplay() {
          if (!this.autoplayMs) return;
          this.stopAutoplay();
          this.timer = setInterval(() => {
            const next = (this.current + 1) % this.slides.length;
            this.goTo(next);
          }, this.autoplayMs);
        }

        stopAutoplay() {
          if (this.timer) clearInterval(this.timer);
          this.timer = null;
        }

        restartAutoplay() {
          if (this.autoplayMs) this.startAutoplay();
        }

        disconnectedCallback() {
          this.stopAutoplay();
          window.removeEventListener('resize', this.onResize);
        }
      }
    );
  }
</script>

{% schema %}
{
  "name": "Testimonios (vertical)",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "header",
      "content": "Textos y tamaños"
    },
    {
      "type": "range",
      "id": "quote_size",
      "label": "Tamaño de la cita (escritorio)",
      "min": 20,
      "max": 72,
      "step": 2,
      "unit": "px",
      "default": 42
    },
    {
      "type": "range",
      "id": "quote_size_mobile",
      "label": "Tamaño de la cita (móvil)",
      "min": 16,
      "max": 44,
      "step": 1,
      "unit": "px",
      "default": 26
    },
    {
      "type": "range",
      "id": "author_size",
      "label": "Tamaño del autor",
      "min": 10,
      "max": 28,
      "step": 1,
      "unit": "px",
      "default": 15
    },
    {
      "type": "range",
      "id": "star_size",
      "label": "Tamaño de estrellas (escritorio)",
      "min": 14,
      "max": 44,
      "step": 2,
      "unit": "px",
      "default": 26
    },
    {
      "type": "range",
      "id": "star_size_mobile",
      "label": "Tamaño de estrellas (móvil)",
      "min": 12,
      "max": 36,
      "step": 2,
      "unit": "px",
      "default": 22
    },
    {
      "type": "checkbox",
      "id": "uppercase",
      "label": "Cita en mayúsculas",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "align_left_mobile",
      "label": "Alinear a la izquierda en móvil",
      "default": true
    },
    {
      "type": "range",
      "id": "max_width",
      "label": "Ancho máximo del contenido",
      "min": 700,
      "max": 1800,
      "step": 20,
      "unit": "px",
      "default": 1200
    },
    {
      "type": "header",
      "content": "Espaciado (escritorio)"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Padding superior",
      "min": 0,
      "max": 200,
      "step": 4,
      "unit": "px",
      "default": 96
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Padding inferior",
      "min": 0,
      "max": 200,
      "step": 4,
      "unit": "px",
      "default": 96
    },
    {
      "type": "range",
      "id": "padding_x",
      "label": "Padding lateral",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "default": 48
    },
    {
      "type": "range",
      "id": "margin_top",
      "label": "Margen superior",
      "min": 0,
      "max": 160,
      "step": 4,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "margin_bottom",
      "label": "Margen inferior",
      "min": 0,
      "max": 160,
      "step": 4,
      "unit": "px",
      "default": 0
    },
    {
      "type": "header",
      "content": "Espaciado (móvil)"
    },
    {
      "type": "range",
      "id": "padding_top_mobile",
      "label": "Padding superior",
      "min": 0,
      "max": 160,
      "step": 4,
      "unit": "px",
      "default": 64
    },
    {
      "type": "range",
      "id": "padding_bottom_mobile",
      "label": "Padding inferior",
      "min": 0,
      "max": 160,
      "step": 4,
      "unit": "px",
      "default": 64
    },
    {
      "type": "range",
      "id": "padding_x_mobile",
      "label": "Padding lateral",
      "min": 0,
      "max": 60,
      "step": 2,
      "unit": "px",
      "default": 20
    },
    {
      "type": "range",
      "id": "margin_top_mobile",
      "label": "Margen superior",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "margin_bottom_mobile",
      "label": "Margen inferior",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "default": 0
    },
    {
      "type": "header",
      "content": "Espacios internos (escritorio)"
    },
    {
      "type": "range",
      "id": "gap_stars",
      "label": "Espacio bajo las estrellas",
      "min": 0,
      "max": 100,
      "step": 2,
      "unit": "px",
      "default": 40
    },
    {
      "type": "range",
      "id": "gap_quote",
      "label": "Espacio bajo la cita",
      "min": 0,
      "max": 100,
      "step": 2,
      "unit": "px",
      "default": 36
    },
    {
      "type": "range",
      "id": "gap_nav",
      "label": "Espacio sobre la navegación",
      "min": 0,
      "max": 120,
      "step": 2,
      "unit": "px",
      "default": 50
    },
    {
      "type": "header",
      "content": "Espacios internos (móvil)"
    },
    {
      "type": "range",
      "id": "gap_stars_mobile",
      "label": "Espacio bajo las estrellas",
      "min": 0,
      "max": 80,
      "step": 2,
      "unit": "px",
      "default": 26
    },
    {
      "type": "range",
      "id": "gap_quote_mobile",
      "label": "Espacio bajo la cita",
      "min": 0,
      "max": 80,
      "step": 2,
      "unit": "px",
      "default": 28
    },
    {
      "type": "range",
      "id": "gap_nav_mobile",
      "label": "Espacio sobre la navegación",
      "min": 0,
      "max": 100,
      "step": 2,
      "unit": "px",
      "default": 36
    },
    {
      "type": "header",
      "content": "Autoplay"
    },
    {
      "type": "checkbox",
      "id": "autoplay",
      "label": "Cambiar de reseña automáticamente",
      "default": false
    },
    {
      "type": "range",
      "id": "autoplay_speed",
      "label": "Segundos entre reseñas",
      "min": 3,
      "max": 12,
      "step": 1,
      "unit": "s",
      "default": 6
    },
    {
      "type": "header",
      "content": "Colores"
    },
    {
      "type": "color",
      "id": "color_bg",
      "label": "Fondo de la sección",
      "default": "#0B0B0B"
    },
    {
      "type": "color",
      "id": "color_text",
      "label": "Texto de la cita",
      "default": "#FFFFFF"
    },
    {
      "type": "color",
      "id": "color_author",
      "label": "Nombre del autor",
      "default": "#FFFFFF"
    },
    {
      "type": "color",
      "id": "color_star",
      "label": "Estrellas",
      "default": "#FFFFFF"
    },
    {
      "type": "color",
      "id": "color_nav",
      "label": "Flechas",
      "default": "#FFFFFF"
    },
    {
      "type": "color",
      "id": "color_nav_border",
      "label": "Borde de las flechas",
      "default": "#5A5A5A"
    },
    {
      "type": "color",
      "id": "color_dot",
      "label": "Punto inactivo",
      "default": "#4A4A4A"
    },
    {
      "type": "color",
      "id": "color_dot_active",
      "label": "Punto activo",
      "default": "#FFFFFF"
    }
  ],
  "blocks": [
    {
      "type": "testimonial",
      "name": "Reseña",
      "settings": [
        {
          "type": "range",
          "id": "rating",
          "label": "Estrellas",
          "min": 0,
          "max": 5,
          "step": 1,
          "default": 5
        },
        {
          "type": "inline_richtext",
          "id": "quote",
          "label": "Cita",
          "info": "Podés poner partes en negrita.",
          "default": "\"Se notan mucho los beneficios!! Lo recomiendo 100%\""
        },
        {
          "type": "text",
          "id": "author",
          "label": "Autor",
          "default": "Borja A."
        }
      ]
    }
  ],
  "max_blocks": 12,
  "presets": [
    {
      "name": "Testimonios (vertical)",
      "blocks": [
        {
          "type": "testimonial",
          "settings": {
            "quote": "\"Se notan mucho los beneficios!! Lo recomiendo 100%\"",
            "author": "Borja A."
          }
        },
        {
          "type": "testimonial",
          "settings": {
            "quote": "“Un tremendo <strong>chute natural</strong>. Cumple con todos y cada uno de sus principios”",
            "author": "Ivan M."
          }
        },
        {
          "type": "testimonial",
          "settings": {
            "quote": "\"Una mejoría increíblemente positiva en la concentración y la tranquilidad mental\"",
            "author": "Silvia R."
          }
        }
      ]
    }
  ]
}
{% endschema %}
